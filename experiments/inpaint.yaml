train:
  snapshot_save_iterations: 5000
  image_save_iterations: 2500
  image_display_iterations: 100
  display: 1
  hyperparameters:
    completion:
      lr: 0.001
      batch_size: 100
      num_epochs: 10
    descriminator:
      lr: 0.001
      batch_size: 100
      num_epochs: 10
  data:
    root: '/home/kapil/Documents/DeepLearning/image-completion-inpainting/data'
    processed: '/home/kapil/Documents/DeepLearning/image-completion-inpainting/data/processed'
    batch_size: 50
    valid_size: 0.1
    random_seed: 0.4
    shuffle: False
    # If using CUDA, turn pin_memory=True and workers=1
    pin_memory: False
    workers: 4
    augment: False
    show_sample: True
